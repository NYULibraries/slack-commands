{
  "name": "slack-commands",
  "version": "0.1.0",
  "description": "NYU Libraries Slack Commands via AWS Lambda",
  "main": "index.js",
  "scripts": {
    "test-and-report": "yarn test && yarn coverage",
    "coverage": "nyc report --reporter=text-lcov | yarn coveralls",
    "test": "yarn nyc jasmine",
    "deploy": "yarn serverless deploy"
  },
  "keywords": [
    "slack",
    "jenkins",
    "slash",
    "commands"
  ],
  "author": "Elliott Grieco",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.2",
    "eslint": "^5.7.0",
    "jasmine": "^3.2.0",
    "jasmine-spec-reporter": "^4.2.1",
    "nyc": "^13.1.0",
    "serverless": "^1.32.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "engines": {
    "node": "^8.10"
  }
}
